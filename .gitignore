# =====================================================================
# intent-sdk .gitignore
#
# ⚠️ 本文件同时承担一道安全防线：仓库工作区里存在若干**不应随开源发布**的内容
#    （内部工具、含真实客户数据的备份 SQL、个人品牌运营稿件）。
#    改动本文件前请先读 §内部内容 的说明。
# =====================================================================

# ---------------------------- 构建产物
target/
*.class
*.jar
*.war
!.mvn/wrapper/maven-wrapper.jar
# 例外：随仓库分发的 pi 本地包。
# dev.pi 制品未发布到任何公共仓库，为了让 clone 后 mvn install 一定能成功，
# 执行器所需的 pi jar 随仓库分发（见 pom.xml 顶部说明与 README「pi 本地包」一节）。
!libs/**/*.jar

# ---------------------------- IDE / 编辑器
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
*.swp

# ---------------------------- 本地运行产物
logs/
*.log
*.log.*
data/
intent-traces/
intent-feedback.jsonl

# ---------------------------- 密钥与本机配置
# 绝不提交任何形式的密钥；API Key 一律走环境变量
# （PI_API_KEY / DEEPSEEK_API_KEY / OPENAI_API_KEY / ANTHROPIC_API_KEY）
*.local.yml
*.local.yaml
application-local.yml
application-local.yaml
.env
.env.*
!.env.example
secrets/
*.pem
*.p12
*.jks

# =====================================================================
# 内部内容（不进开源仓）
# =====================================================================

# 内部工具与调试残留：node_modules、构建日志、临时 e2e 脚本，
# 以及 backup-*.sql —— 那是从**线上 CRM**导出的真实跟进记录（含真实客户名与决策人），
# 属于客户数据，绝不允许进版本库。要保留请移到仓库之外的私有目录。
tools/

# 个人品牌运营内容：公众号推文、软文、发布信息（含个人微信号），
# 属于对外内容运营，与技术仓分离维护。
docs/公众号/
docs/软文-*.md
docs/发布信息.md

# 未定稿的设计草稿（知识库/本体防腐层的 ACL 草案），定稿后再单独提 PR
docs/draft-acl/

# 演示视频素材（体积大，走 GitHub Releases 分发）
demo-video/
